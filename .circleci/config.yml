# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  build-android:
    # Specify the execution environment. You can specify an image from Dockerhub, or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: alvrme/alpine-android:android-33-jdk11-v2023.06.10
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - run:
            name: "Make gradlew executable"
            command: "chmod +x gradlew"
      - run:
            name: "Build Project"
            command: "./gradlew :android:packageRelease"
            environment:
              CIRCLE_CI: true
      - store_artifacts:
          name: "Store Android Artifacts"
          path: android/build/outputs/apk/release/
  build-desktop:
    # Specify the execution environment. You can specify an image from Dockerhub, or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: eclipse-temurin:17.0.7_7-jdk
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - run:
            name: "Make gradlew executable"
            command: "chmod +x gradlew"
      - run:
            name: "Build Project"
            command: "./gradlew :desktop-merge:build :desktop-merge:jpackage --info"
            environment:
              CIRCLE_CI: true
      - store_artifacts:
          name: "Store Desktop Binaries"
          path: desktop-merge/build/dist/
      - store_artifacts:
          name: "Store Desktop Artifacts"
          path: desktop/build/libs/

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  build-workflow:
    jobs:
      - build-android
      - build-desktop
